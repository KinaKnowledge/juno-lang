;; Initialize and load the test namespace


(when (not (contains? "tests" (namespaces)))
  (create_namespace `tests))
(progn
 (declare (namespace `tests))
 (import "tests/compiler-tests-1.lisp")
 (import "tests/test_harness.lisp"))
